# Vexon 4.0 — κατάσταση παράδοσης

Η έκδοση αυτή είναι κώδικας προς Windows build. Δεν περιλαμβάνει νέο, δοκιμασμένο EXE. Οι τοπικές δοκιμές χρησιμοποιούν ελεγχόμενες απαντήσεις API· η νέα έκδοση χρειάζεται desktop smoke test σε Windows πριν διανομή.

## Λειτουργίες

1. WordPress: ορατό meta generator και same-origin διαδρομές plugin/theme από assets. Αναζήτηση έως 5 στοιχείων στο NVD. Οι εκδόσεις από asset URLs είναι ενδείξεις, οι αναφορές NVD υποψήφιες. Δεν γίνεται πλήρης απογραφή εγκατεστημένων plugins ούτε αυτόματη απόδειξη ότι μια έκδοση εμπίπτει σε όλα τα affected ranges.
2. Login scan: στο chat, επιλογή έργου και εισαγωγή Cookie header δοκιμαστικού λογαριασμού. Παραμένει μόνο στη μνήμη για έως 1 ώρα, σε χειροκίνητα HTTPS scans στο ίδιο origin. Δεν αυτοματοποιείται η φόρμα σύνδεσης ή το MFA. Τα credentials δεν περνούν σε GitHub/OSV. Η ισχύς του login δεν επιβεβαιώνεται αυτόματα, επομένως η κάλυψη χαρακτηρίζεται μερική.
3. Εξαρτήσεις: npm package-lock.json, PHP composer.lock και ακριβώς pinned Python requirements.txt. Εξαιρούνται Python environment markers, ranges, editable installs. Μέχρι 300 εκδόσεις συνολικά, μέσα στα 60 αρχεία του source scanner.
4. Επιβεβαίωση: δεύτερη HTTP παρατήρηση για έως 3 διευθύνσεις της πλήρους σάρωσης. Ασυνεπή αποτελέσματα χρειάζονται έλεγχο. Δεν επιβεβαιώνεται εκμεταλλευσιμότητα.
5. Chat: ζωντανά βήματα πραγματικών σαρώσεων ανά έργο. Τα πρόσφατα συμβάντα διατηρούνται στη μνήμη της τρέχουσας προβολής.
6. Σύγκριση: προηγούμενα και νέα τεκμήρια, με διάκριση νέου, επίμονου, αλλαγμένου και μη επαληθευμένου ευρήματος. Διαφορετικό δημόσιο/login context δεν επιβεβαιώνει επίλυση.
7. Προτάσεις: export αρχείου .patch για nginx/Apache X-Content-Type-Options και Referrer-Policy. Δημιουργεί snippets στον φάκελο vexon-proposals για έλεγχο σε staging. Δεν αλλάζει αυτόματα παραγωγική ρύθμιση και δεν ανοίγει PR.
8. CVE watch: επανελέγχει την τελευταία αποθηκευμένη απογραφή OSV, χειροκίνητα ή ημερησίως όσο λειτουργεί η εφαρμογή. Στην πρώτη εκτέλεση οι υπάρχουσες αντιστοιχίσεις αποτελούν την αρχική καταγραφή. Η ημερομηνία απογραφής εμφανίζεται ώστε να μην εκληφθεί ως νέα απογραφή παραγωγής.
9. Backup: AES-256-GCM με κλειδί από scrypt. Έργα, αναφορές, συνομιλίες και έρευνα. Περιλαμβάνει επωνυμία, στοιχεία και λογότυπο PDF. Δεν περιλαμβάνει API keys, cookies ή ρύθμιση αυτόματης εκκίνησης Windows. Εισαγωγή χωρίς overwrite· σε ίδιο ID η εισαγωγή απορρίπτεται. Τα εισαγόμενα έργα χρειάζονται ξανά εξουσιοδότηση και έχουν απενεργοποιημένους προγραμματισμούς.
10. Ενημερώσεις: ενσωματωμένος electron-updater, ενεργός μόνο σε εγκατεστημένο build με publisherName. Χρειάζεται πραγματικό πιστοποιητικό υπογραφής Windows και δημοσιευμένο GitHub release. Το workflow release-signed.yml απαιτεί WINDOWS_CERTIFICATE και WINDOWS_CERTIFICATE_PASSWORD στα GitHub secrets. Δημιουργεί υπογεγραμμένο installer και metadata χωρίς να υποβαθμίζει την επαλήθευση υπογραφής. Οι υφιστάμενοι χρήστες unsigned εκδόσεων πρέπει να εγκαταστήσουν την πρώτη signed έκδοση χειροκίνητα. Δεν έχουν παρασχεθεί πιστοποιητικό ή secrets στην παρούσα εργασία.

## Build σε Windows

Απαιτείται Node 22 ή νεότερο μόνο για το build:

```powershell
npm ci
npm test
npm run test:desktop
npm run build:win
```

Το EXE δημιουργείται στο dist. Ο τελικός χρήστης δεν χρειάζεται Node. Για ενημέρωση του GitHub, ανέβασε όλο το περιεχόμενο του source ZIP, συμπεριλαμβανομένου του .github, χωρίς node_modules/dist. Το Windows CI πρέπει να ολοκληρωθεί πριν θεωρηθεί έτοιμος ο installer.
